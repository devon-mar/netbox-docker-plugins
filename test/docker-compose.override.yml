---
version: "3.4"
services:
  netbox:
    ports:
      - 8000:8080
    build:
      context: .
      dockerfile: Dockerfile
    image: netbox:plugins
  netbox-worker:
    image: netbox:plugins
